# TICKET-ADV150 — Provider definition (skeleton)
#
# TODO: Configure Grafana to auto-load dashboard JSON files from:
#   /etc/grafana/provisioning/dashboards
apiVersion: 1
providers: []
# TODO: add file provider pointing at /etc/grafana/provisioning/dashboards
