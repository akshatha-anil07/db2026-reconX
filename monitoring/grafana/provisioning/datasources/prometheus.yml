# TICKET-ADV150 — Auto-provision the Prometheus datasource (skeleton)
#
# TODO: Configure Grafana to auto-provision Prometheus as a datasource.
#   - url: http://prometheus:9090  (use service name, NOT localhost)
#   - set as default datasource
#   - uid: reconx-prometheus (referenced by dashboard JSON)
apiVersion: 1
datasources: []
# TODO: add datasource config
