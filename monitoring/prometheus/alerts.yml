# TICKET-ADV093 / ADV094 — Prometheus alert rules (skeleton)
#
# TODO: Define at least two alert rules:
#   1. TooManyReconBreaks — fires when recon_break_count > 50 for 5m
#   2. HighApiLatencyP95  — fires when P95 latency > 500ms for 3m
#   3. KafkaDlqGrowing    — fires when messages appear in trade-events-dlq
groups:
  - name: reconx-business
    rules: []
    # TODO: add alert rules here
