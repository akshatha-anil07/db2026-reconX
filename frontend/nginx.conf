server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # TODO(TICKET-ADV147): Add SPA fallback so React Router deep links work.
    # location / { try_files $uri /index.html; }

    # TODO(TICKET-ADV147): Add API proxy to backend service
    # location /api/ { proxy_pass http://backend:8080/api/; }
}
